package `in`.francl.cam.application.port.outbound.authorization

interface TokenExpirable : `in`.francl.cam.application.port.outbound.authorization.Tokenable,
    `in`.francl.cam.application.port.outbound.authorization.Expirable {
}